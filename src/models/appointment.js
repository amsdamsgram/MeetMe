define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone){
    var Appointment = Backbone.Model.extend({

        initialize: function(){
        }
    });
    return Appointment;
});